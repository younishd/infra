ingress:
  enabled: true
  ingressClassName: nginx-tailnet
  hostname: "tx.dev.{{ .Values.spec.baseDomain }}"
  annotations:
    cert-manager.io/cluster-issuer: {{ .Values.spec.clusterIssuer }}

persistence:
  enabled: true
  existingClaim: plex-volume

existingSecret: transmission-secret
