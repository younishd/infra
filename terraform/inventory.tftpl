all:
  children:
    control_plane:
      hosts:
%{ for v in control_plane ~}
        ${v.name}:
          node_address_ipv4: "${v.address_ipv4}"
%{ endfor ~}

    data_plane:
      hosts:
%{ for v in data_plane ~}
        ${v.name}:
          node_address_ipv4: "${v.address_ipv4}"
%{ endfor ~}
